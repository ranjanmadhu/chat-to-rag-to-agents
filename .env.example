# Choose the default LLM provider: ollama or gemini
LLM_PROVIDER=gemini

# Comma-separated list of allowed providers (for Azure dev-demo use: gemini)
ENABLED_PROVIDERS=gemini,ollama

# Gemini settings (optional unless using Gemini)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
